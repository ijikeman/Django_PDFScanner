{
    "name": "Django Application",
    "build": {
        "dockerfile": "Dockerfile",
    },
    "mounts": [ // dockerマウント設定 /workspace/にマウントする(default)
        "source=${localWorkspaceFolder}/,target=/workspace/,type=bind,consistency=delegated"
    ],
}
